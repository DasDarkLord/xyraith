event startup {
    foreach :index (range 1 10) {
        console.log (call :compare (load :index))
    }
}

function :compare {
    if (le (parameter 0) 5) {
        return (string (parameter 0) " is less than 5.")
    }
    return (string (parameter 0) " is greater than or equal to 5!")
}