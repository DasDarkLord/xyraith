(event startup (
    (world.setBlock (vec 0 68 0) "minecraft:iron_block")
))

(event command (
    (player.sendMessage "<red>Thanks for the command! We love you!")
    (player.sendMessage "<blue>You are at Y level...")
    (player.sendMessage (loc.getY (player.getLoc)))
))