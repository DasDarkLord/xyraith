function :player.setGamemode (string -> nothing) {
    if (eq (parameter 0) "survival") {
        unsafe.player.setGamemode 0
    }
    if (eq (parameter 0) "creative") {
        unsafe.player.setGamemode 1
    }
}